# curriculum_nav_drift: ?

Repo: `ai-infra-senior-engineer-solutions`
Target: `CURRICULUM.md`

## Why this work exists

`CURRICULUM.md` does not reference module `mod-201-advanced-kubernetes` that exists on disk.

## Scope

- Edit only the files needed to address the issue above.
- Do NOT touch CURRICULUM.md sections you weren't asked to,
  do NOT touch VERSIONS.md (the runner manages that).
- Preserve all existing structure and tone.

## Specific instructions

The named nav doc is out of sync with what's on disk. If a module/project is missing from the doc, add a row in the appropriate table preserving the existing column shape. If a referenced module/project doesn't exist on disk, remove the row. Do NOT invent content — just align the navigation.